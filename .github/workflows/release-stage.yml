name: Release Stage

on:
  workflow_dispatch:
    inputs:
      rc_version:
        description: 'RC version to promote (e.g., 1.0.0-rc.47, or leave blank for latest RC)'
        required: false
        type: string

jobs:
  promote-rc-to-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: read
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Fetch all history and tags for version checking
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x
    
    - name: Find latest RC version if not provided
      id: find-rc
      uses: ./.github/actions/find-latest-nuget-rc-version
      with:
        rc-version: ${{ github.event.inputs.rc_version }}
        github-token: ${{ secrets.GITHUB_TOKEN }}
        package-name: 'Optivem.Testing'
    
    - name: Validate RC version and extract release version
      id: validate
      uses: ./.github/actions/validate-rc-version
      with:
        rc-version: ${{ steps.find-rc.outputs.rc-version }}
    
    - name: Check if release version already exists on NuGet.org
      uses: ./.github/actions/check-nuget-package-exists
      with:
        release-version: ${{ steps.validate.outputs.release-version }}
        package-name: 'Optivem.Testing'
    
    - name: Check if Git tag already exists
      uses: ./.github/actions/check-tag-exists
      with:
        tag-name: v${{ steps.validate.outputs.release-version }}
    
    - name: Download RC package from GitHub Packages
      uses: ./.github/actions/download-nuget-rc-artifacts
      with:
        rc-version: ${{ steps.validate.outputs.rc-version }}
        github-username: ${{ github.actor }}
        github-token: ${{ secrets.GITHUB_TOKEN }}
        package-name: 'Optivem.Testing'
    
    - name: Repack with release version
      uses: ./.github/actions/prepare-nuget-release-artifacts
      with:
        rc-version: ${{ steps.validate.outputs.rc-version }}
        release-version: ${{ steps.validate.outputs.release-version }}
        package-name: 'Optivem.Testing'
    
    - name: Publish to NuGet.org
      uses: ./.github/actions/publish-nuget
      with:
        package-path: './temp-release/Optivem.Testing.${{ steps.validate.outputs.release-version }}.nupkg'
        nuget-api-key: ${{ secrets.NUGET_API_KEY }}
    
    - name: Wait for NuGet.org indexing
      uses: ./.github/actions/wait-for-nuget-indexing
      with:
        package-name: 'Optivem.Testing'
        version: ${{ steps.validate.outputs.release-version }}
        max-retries: '30'
        interval-seconds: '10'

    - name: Restore release smoke test dependencies (retry)
      uses: ./.github/actions/dotnet-restore-with-retry
      with:
        project-path: SystemTest/Optivem.Testing.SystemTest.SmokeTestRelease/Optivem.Testing.SystemTest.SmokeTestRelease.csproj
        version: ${{ steps.validate.outputs.release-version }}
        max-retries: '5'
        interval-seconds: '30'
    
    - name: Run release smoke tests (NuGet.org)
      run: |
        echo "🧪 Running release smoke tests..."
        dotnet test SystemTest/Optivem.Testing.SystemTest.SmokeTestRelease/Optivem.Testing.SystemTest.SmokeTestRelease.csproj --no-restore -p:Version="${{ steps.validate.outputs.release-version }}"
    
    - name: Create Git tag
      uses: ./.github/actions/create-tag
      with:
        tag-name: v${{ steps.validate.outputs.release-version }}
    
    - name: Create GitHub Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: v${{ steps.validate.outputs.release-version }}
        name: Release ${{ steps.validate.outputs.release-version }}
        body: |
          🎉 **Release ${{ steps.validate.outputs.release-version }}**
          
          This release was promoted from RC version `${{ steps.validate.outputs.rc-version }}` after passing all acceptance tests.
          
          **Promoted Artifacts:**
          - Same exact package that passed testing
          - No rebuild - identical artifacts guaranteed
          - ✅ Passed release smoke tests on NuGet.org
          - ✅ Automatically published to NuGet.org
          
          **Installation:**
          ```bash
          dotnet add package Optivem.Testing --version ${{ steps.validate.outputs.release-version }}
          ```
          
          **NuGet Package:**
          https://www.nuget.org/packages/Optivem.Testing/${{ steps.validate.outputs.release-version }}
          
          **Note:** NuGet.org synchronization may take a few minutes.
        generate_release_notes: true
        draft: false
        prerelease: false
    
    - name: Display next steps
      shell: pwsh
      run: |
        $releaseVersion = "${{ steps.validate.outputs.release-version }}"
        Write-Host ""
        Write-Host "🎉 Release promotion completed successfully!" -ForegroundColor Green
        Write-Host ""
        Write-Host "📋 What happened:" -ForegroundColor Cyan
        Write-Host "1. ✅ Downloaded RC package from GitHub Packages" -ForegroundColor White
        Write-Host "2. ✅ Repacked and published to NuGet.org (same artifacts, no rebuild)" -ForegroundColor White
        Write-Host "3. ✅ Waited for NuGet.org indexing and ran smoke tests" -ForegroundColor White
        Write-Host "4. ✅ Created Git tag v${releaseVersion}" -ForegroundColor White
        Write-Host "5. ✅ Created GitHub Release" -ForegroundColor White
        Write-Host ""
        Write-Host "📋 Next Steps:" -ForegroundColor Cyan
        Write-Host "1. ⏳ Wait a few minutes for NuGet.org synchronization" -ForegroundColor White
        Write-Host "2. 🧪 Verify: https://www.nuget.org/packages/Optivem.Testing/${releaseVersion}" -ForegroundColor White
        Write-Host ""
        Write-Host "✅ Same artifacts that passed RC testing are now released!" -ForegroundColor Green
    
    - name: Create release summary
      run: |
        echo "## 🎉 Release Stage Completed" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Release Version:** \`${{ steps.validate.outputs.release-version }}\`" >> $GITHUB_STEP_SUMMARY
        echo "**Promoted from RC:** \`${{ steps.validate.outputs.rc-version }}\`" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "🚀 **Release Activities:**" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Downloaded RC package from GitHub Packages" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Repacked and published to NuGet.org" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Waited for NuGet.org indexing" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Ran release smoke tests" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Created Git tag v${{ steps.validate.outputs.release-version }}" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Created GitHub Release" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Status:** 🎉 Release published successfully!" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Next Steps:**" >> $GITHUB_STEP_SUMMARY
        echo "- ⏳ Wait for NuGet.org synchronization (may take a few minutes)" >> $GITHUB_STEP_SUMMARY
        echo "- 🧪 Verify: https://www.nuget.org/packages/Optivem.Testing/${{ steps.validate.outputs.release-version }}" >> $GITHUB_STEP_SUMMARY
